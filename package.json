{
	"type": "module",
	"name": "@donutteam/pure3d",
	"description": "This is a TypeScript library designed to read Pure3D files from video games created by Radical Entertainment.",
	"version": "1.1.0",
	"main": "./build/index.js",
	"scripts": {
		"build": "npx rimraf build && npx tsc",
		"build:watch": "npx rimraf build && npx tsc --watch",
		"prepublishOnly": "npm run build",
		"test": "node --test"
	},
	"keywords": [
		"pure3d"
	],
	"author": "Donut Team",
	"license": "MIT",
	"peerDependencies": {
		"@donutteam/binary-rw": "^2.0.0"
	},
	"devDependencies": {
		"@donutteam/typescript-config": "^1.0.0",
		"@types/node": "^20.12.7",
		"@types/source-map-support": "^0.5.10",
		"rimraf": "^5.0.5",
		"sharp": "^0.33.3",
		"source-map-support": "^0.5.21",
		"typescript": "^5.4.5"
	}
}
